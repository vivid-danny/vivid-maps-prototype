
  # Build map section component

  This is a code bundle for Build map section component. The original project is available at https://www.figma.com/design/8msJAvddL4ZmPVDfPWbpA3/Build-map-section-component.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  